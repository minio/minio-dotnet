language: csharp
dist: trusty
sudo: required
mono: none
dotnet: 1.0.3
branches:
  only:
    - master
install:

script:
- dotnet restore /p:Configuration=.netcore 
- dotnet publish  --output out /p:Configuration=.netcore
- dotnet /home/travis/build/minio/minio-dotnet/Minio.Functional.Tests/bin/Debug/netcoreapp1.1/Minio.Functional.Tests.dll
