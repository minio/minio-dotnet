language: csharp
dist: trusty
sudo: required
mono: none
dotnet: 1.1.5
branches:
  only:
    - master
install:

script:
- dotnet restore
- dotnet publish --output out /p:Configuration=Release
- dotnet Minio.Functional.Tests/bin/Release/netcoreapp2.0/Minio.Functional.Tests.dll
