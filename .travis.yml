language: csharp
dist: trusty
sudo: required
mono: none
dotnet: 2.1.4
branches:
  only:
    - master
install:

script:
- dotnet restore
- dotnet build  Minio.Functional.Tests -c Release --framework netcoreapp2.0
- dotnet run Minio.Functional.Tests/bin/Release/netcoreapp2.0/Minio.Functional.Tests.dll
